function prepareValidation(rules){
    /*alert('inside');
    var validationRules = String(rules).split(",");
    var fieldName = "";
    validationRules.forEach(function(entry){
    	alert(entry);

    	
    })
    alert(validationRules);*/
}